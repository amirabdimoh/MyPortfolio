-- Run this file to create the database and tables
-- In pgAdmin: Tools → Query Tool → Open File → Execute

-- Create database (run this first, then connect to dashboard_db and run the rest)
CREATE DATABASE dashboard_db;

-- After creating database, connect to it and run the schema below
-- Or run: psql -U postgres -d dashboard_db -f config/database.sql
